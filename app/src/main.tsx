import{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import axios from'axios';import'./mvp.css';import{InvestmentForm}from'./InvestmentForm';const api=axios.create({baseURL:'http://localhost:5147'});type S={id:string,name:string,sector:string,certificationStatus:string,trustLevel:number,growthRate:number,totalRaised:number,activeInvestors:number,averageRoi:number,retentionRate:number,mrr:number,profitMargin:number,description:string,financialSeries:number[]};type R={id:string;smeId:string;company:string;sector:string;submittedAt:string;status:string;documents:string[];reason?:string};const B=({s}:{s:string})=><b className={'b '+s}>{s==='Certified'?'Certificada':s==='UnderReview'?'En revisión':'Rechazada'}</b>;const allowed=['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.ms-excel','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','image/png','image/jpeg'];function Login({go}:{go:(r:string)=>void}){const[r,set]=useState('Investor');const[e,setE]=useState('');const accounts=['Investor','SME','Expert'] as const;return <main className="login"><section className="login-brand"><div className="brand-mark">↗</div><h1>QuietWealth</h1><p className="brand-lead">Plataforma fintech que conecta PYMEs certificadas con inversionistas de confianza.</p><div className="benefit"><b/><span><strong>Certificación por expertos</strong><small>Validación financiera transparente</small></span></div><div className="benefit"><b/><span><strong>Marketplace transparente</strong><small>Acceso a PYMEs verificadas</small></span></div><div className="benefit"><b/><span><strong>Inversión informada</strong><small>Datos financieros estandarizados</small></span></div><em>© 2026 QuietWealth. Todos los derechos reservados.</em></section><div className="login-right"><section className="login-panel"><h2>Bienvenido a QuietWealth</h2><p>Inicia sesión con tu cuenta corporativa</p><label>Selecciona tu tipo de cuenta:</label><div className="role-picker">{accounts.map(role=><button key={role} className={r===role?'selected':''} onClick={()=>set(role)}><strong>{role==='SME'?'PYME':role}</strong></button>)}</div><button className="microsoft-login" onClick={async()=>{try{await api.post('/api/local-auth/login',{email:r.toLowerCase()+'@quietwealth.test',role:r});go(r)}catch{setE('No fue posible iniciar la sesión local.')}}}><span className="ms-squares"><i/><i/><i/><i/></span>Continuar con Microsoft</button><div className="sso-note"><strong>¿Por qué Microsoft?</strong><span>• Inicio de sesión corporativo<br/>• Acceso seguro y centralizado<br/>• Sin contraseñas en esta demo</span></div>{e&&<p className="login-error">{e}</p>}</section><p className="terms-copy">Al iniciar sesión, aceptas nuestros <a>Términos de Servicio</a> y <a>Política de Privacidad</a>.</p></div></main>}function Market(){const[x,setX]=useState<S[]>([]),[q,setQ]=useState(''),[sector,setSector]=useState(''),[status,setStatus]=useState(''),[trust,setTrust]=useState('');useEffect(()=>{api.get('/api/marketplace',{params:{search:q,sector,certificationStatus:status,trustLevel:trust}}).then(r=>setX(r.data))},[q,sector,status,trust]);return <main><small>MARKETPLACE</small><h1>Empresas con información verificada</h1><div className="filters"><input placeholder="Buscar empresa" value={q} onChange={e=>setQ(e.target.value)}/><select value={sector} onChange={e=>setSector(e.target.value)}><option value="">Todos los sectores</option><option>Technology</option><option>Energy</option><option>Commerce</option></select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Toda certificación</option><option value="Certified">Certificada</option><option value="UnderReview">En revisión</option><option value="Rejected">Rechazada</option></select><select value={trust} onChange={e=>setTrust(e.target.value)}><option value="">Todo trust level</option><option value="80">80+ alto</option><option value="60">60+ medio</option></select></div><div className="grid">{x.map(a=><section key={a.id}><B s={a.certificationStatus}/><h2>{a.name}</h2><p>{a.sector} · Trust {a.trustLevel}/100</p><p className="project-description">{a.description}</p><p>↑ {a.growthRate}% · ${a.totalRaised.toLocaleString()} · {a.activeInvestors} inversionistas</p><button onClick={()=>location.hash='/marketplace/'+a.id}>Ver detalles</button></section>)}</div>{!x.length&&<p>No hay empresas que coincidan con los filtros.</p>}</main>}function Detail({id}:{id:string}){const[x,set]=useState<S>();useEffect(()=>{api.get('/api/marketplace/'+id).then(r=>set(r.data))},[id]);if(!x)return <main>Cargando…</main>;return <main><button onClick={()=>location.hash='/marketplace'}>← Marketplace</button><section className="hero"><div><B s={x.certificationStatus}/><h1>{x.name}</h1><div className="project-summary"><small>Sobre el proyecto</small><p>{x.description}</p></div><strong>Trust record: {x.trustLevel}/100 · Revisado por experto</strong></div><div className="hero-actions"><button className="invest-now" onClick={()=>location.hash='/invest/'+x.id}>Invertir ahora</button></div></section><div className="stats">{[['Total Raised','$'+x.totalRaised.toLocaleString()],['Active Investors',x.activeInvestors],['Growth Rate',x.growthRate+'%'],['Average ROI',x.averageRoi+'%']].map(a=><section key={String(a[0])}><small>{a[0]}</small><h2>{a[1]}</h2></section>)}</div><section className="analytics"><h2>Indicadores de crecimiento verificados</h2><div className="chart-pair"><div className="visual-card"><h3>Crecimiento de Ingresos</h3><div className="line-chart"><svg viewBox="0 0 100 50" preserveAspectRatio="none"><path className="income-area" d="M0,42 C15,38 29,35 42,31 S69,19 100,5 L100,50 L0,50Z"/><path className="income-line" d="M0,42 C15,38 29,35 42,31 S69,19 100,5"/></svg></div><small>Ingresos mensuales verificados</small></div><div className="visual-card"><h3>Crecimiento de Inversionistas</h3><div className="area-chart"><svg viewBox="0 0 100 50" preserveAspectRatio="none"><path d="M0,45 C18,40 35,35 52,27 S80,15 100,5 L100,50 L0,50Z"/></svg></div><small>Participación de inversionistas activos</small></div></div><div className="visual-card capital"><h3>Capital Acumulado en el Tiempo</h3><div className="capital-chart"><svg viewBox="0 0 100 45" preserveAspectRatio="none"><path d="M0,42 C17,36 34,32 50,26 S76,10 100,4 L100,45 L0,45Z"/></svg></div><small>Capital acumulado de inversión (USD)</small></div><p>Retención {x.retentionRate}% · MRR ${x.mrr.toLocaleString()} · Margen {x.profitMargin}%</p></section><div className="detail-grid"><section><h2>Descripción de la Empresa</h2><p>{x.description}</p><p>Esta empresa mantiene un expediente financiero estandarizado y evidencia validada por expertos para facilitar decisiones de inversión informadas.</p></section><section><h2>Métricas Clave</h2><dl><div><dt>Inversionistas activos</dt><dd>{x.activeInvestors}</dd></div><div><dt>Tasa de retención</dt><dd>{x.retentionRate}%</dd></div><div><dt>Ingresos recurrentes (MRR)</dt><dd>${x.mrr.toLocaleString()}</dd></div><div><dt>Margen de beneficio</dt><dd>{x.profitMargin}%</dd></div></dl></section></div></main>}function Docs(){const[f,set]=useState<FileList|null>(),[m,setM]=useState(''),[reqs,setReqs]=useState<R[]>([]);const load=()=>api.get('/api/validation/requests').then(r=>setReqs(r.data.filter((x:R)=>x.smeId==='00000000-0000-0000-0000-000000000004')));useEffect(()=>{void load()},[]);const upload=async()=>{const files=Array.from(f||[]);if(!files.length)return setM('Selecciona al menos un documento.');if(files.some(x=>!allowed.includes(x.type)||x.size>10485760))return setM('Solo PDF, DOC/DOCX, XLS/XLSX, PNG o JPG, hasta 10 MB.');try{for(const x of files){const dataUrl=await new Promise<string>(resolve=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.readAsDataURL(x)});await api.post('/api/local-files/upload',{smeId:'00000000-0000-0000-0000-000000000004',fileName:x.name,size:x.size,contentType:x.type,dataUrl});}setM('Información cargada y enviada a revisión.');load()}catch{setM('No fue posible crear la solicitud.')}};return <main className="document-page"><h1>Portal de Carga de Documentos</h1><p className="page-subtitle">Sube tus estados financieros y documentos de identidad</p><section className="upload-shell"><div className="progress-flow"><div className="active"><b>⇧</b><span>Información<br/>Cargada</span></div><i/><div><b>◷</b><span>En Revisión por<br/>Expertos</span></div><i/><div><b>✓</b><span>Certificación Emitida</span></div></div><label className="upload-zone"><input type="file" multiple accept=".pdf,.doc,.docx,.xls,.xlsx,.png,.jpg,.jpeg" onChange={e=>set(e.target.files)}/><strong>⇧</strong><h2>Arrastra tus archivos aquí</h2><span>o</span><em>Seleccionar Archivos</em><small>Formatos aceptados: PDF, DOC, XLS, IMG (máx. 10MB)</small></label>{f&&<p className="selected-files">{Array.from(f).map(x=>x.name).join(', ')}</p>}<button className="submit-upload" onClick={upload}>Enviar para revisión</button>{m&&<p className="upload-message">{m}</p>}</section><h2 className="history-title">Historial de solicitudes</h2>{reqs.map(a=><section className="row" key={a.id}><B s={a.status}/><b>{new Date(a.submittedAt).toLocaleDateString()}</b><span>{a.documents.join(', ')}</span><span>{a.reason||'Pendiente de decisión'}</span></section>)}</main>}function Valid(){const[x,set]=useState<R[]>([]),[selected,setSelected]=useState<R>(),[reason,setReason]=useState('');const load=()=>api.get('/api/validation/requests').then(r=>set(r.data));useEffect(()=>{void load()},[]);const decide=async(ok:boolean)=>{if(!selected)return;await api.post('/api/validation/requests/'+selected.id+'/'+(ok?'approve':'reject'),ok?{}:{reason:reason||'Información financiera insuficiente'});setSelected(undefined);setReason('');load()};return <main><small>EXPERT REVIEW</small><h1>Panel de validación</h1>{x.map(a=><section className="row" key={a.id}><span><B s={a.status}/></span><b>{a.company}</b><span>{a.sector}</span><button onClick={()=>setSelected(a)}>Revisar</button></section>)}{selected&&<div className="modal"><section><button className="close" onClick={()=>setSelected(undefined)}>×</button><small>SOLICITUD {selected.id}</small><h2>{selected.company}</h2><p>{selected.sector} · {new Date(selected.submittedAt).toLocaleDateString()}</p><h3>Documentos recibidos</h3><ul>{selected.documents.map(d=><li key={d}><a href={'http://localhost:5147/api/local-files/open?fileName='+encodeURIComponent(d)} target="_blank" rel="noreferrer">Abrir {d}</a></li>)}</ul>{selected.status==='UnderReview'?<><textarea placeholder="Motivo de rechazo (opcional)" value={reason} onChange={e=>setReason(e.target.value)}/><div><button onClick={()=>decide(true)}>Aprobar certificación</button><button className="reject" onClick={()=>decide(false)}>Rechazar</button></div></>:<p>Decisión: {selected.reason||selected.status}</p>}</section></div>}</main>}function App(){const[r,set]=useState('');const[,setRoute]=useState(location.hash);useEffect(()=>{const update=()=>setRoute(location.hash);addEventListener('hashchange',update);return()=>removeEventListener('hashchange',update)},[]);const p=location.hash.slice(1)||'/login';if(!r)return <Login go={a=>{set(a);location.hash=a==='Investor'?'/marketplace':a==='SME'?'/documents':'/validation'}}/>;const allowedPath=r==='Investor'?(p==='/marketplace'||p.startsWith('/marketplace/')||p.startsWith('/invest/')):r==='SME'?p==='/documents':p==='/validation';if(!allowedPath){location.hash=r==='Investor'?'/marketplace':r==='SME'?'/documents':'/validation';return null}return <><header className="sidebar"><div><b>QuietWealth</b><small>Fintech Platform</small></div><nav><button onClick={()=>location.hash='/marketplace'} disabled={r!=='Investor'}>⊞ &nbsp; Marketplace <small>Investor</small></button><button onClick={()=>location.hash='/documents'} disabled={r!=='SME'}>⇧ &nbsp; Cargar Documentos <small>SME</small></button><button onClick={()=>location.hash='/validation'} disabled={r!=='Expert'}>☑ &nbsp; Panel de Validación <small>Expert</small></button><button onClick={()=>location.hash='/marketplace'} disabled={r!=='Investor'}>↗ &nbsp; Detalle de Inversión <small>Investor</small></button></nav><button className="logout" onClick={async()=>{await api.post('/api/local-auth/logout').catch(()=>{});set('');location.hash='/login'}}>Cerrar sesión</button><em>© 2026 QuietWealth</em></header><div className="app-content">{p==='/marketplace'?<Market/>:p.startsWith('/marketplace/')?<Detail id={p.split('/')[2]}/>:p.startsWith('/invest/')?<InvestmentForm id={p.split('/')[2]}/>:p==='/documents'?<Docs/>:<Valid/>}</div></>}createRoot(document.getElementById('root')!).render(<App/>);


















