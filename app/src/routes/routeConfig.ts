export const routes = {
  login: "/login",
  terms: "/terms",
  privacy: "/privacy",
  marketplace: "/marketplace",
  marketplaceDetail: "/marketplace/:id",
  invest: "/invest/:id",
  documents: "/documents",
  validation: "/validation",
} as const;
